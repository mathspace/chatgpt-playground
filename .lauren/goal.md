Show token, cost and other usage stats as returned by LLM API after a response is received.

- A dev server is running at http://127.0.0.1:8001/
- You MUST test your changes using Playwright MCP.
- If you need API key, it's stored in the .env file.
- Do NOT change endpoint URL.
